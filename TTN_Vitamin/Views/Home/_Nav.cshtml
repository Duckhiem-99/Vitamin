@model IEnumerable<TTN_Vitamin.Models.DanhMucCon>

<nav class="navbar navbar-expand-lg" id="nav-mainmenu" style="background-color:aqua">
    <div class="container">
        <a class="navbar-brand" href="/">
            <span>
                <i class="fas fa-home fa-lg" id="nav-name"></i>
            </span>
        </a>
        <div class="collapse navbar-collapse" style="color:#EEE">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="btn dropdown-toggle" data-toggle="dropdown" role="button" id="nav-name">
                        Bánh kẹo nhập khẩu
                    </a>
                    <ul class="dropdown-menu" style="width:50px;">
                        @foreach (var item in Model.Where(x => x.maDM == "MDM01"))
                        {
                            var child = Model.Where(x => x.maDM == item.maDM);
                            if (child.Count() > 0)
                            {
                                <li>
                                    <a href="@Url.Action("Index","Home",new { id = item.maDMC})" class="link link--yaku"><span>@Html.DisplayFor(model => item.tenDMC)</span></a>
                                </li>
                            }

                        }
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="btn dropdown-toggle" data-toggle="dropdown" role="button" id="nav-name">
                        Snack
                    </a>
                    <ul class="dropdown-menu" style="width:50px;">
                        @foreach (var item in Model.Where(x => x.maDM == "MDM02"))
                        {
                            var child = Model.Where(x => x.maDM == item.maDM);
                            if (child.Count() > 0)
                            {
                                <li>
                                    <a href="@Url.Action("Index","Home",new { id = item.maDMC})" class="link link--yaku"><span>@Html.DisplayFor(model => item.tenDMC)</span></a>
                                </li>
                            }

                        }
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="btn dropdown-toggle" data-toggle="dropdown" role="button" id="nav-name">
                        Nước giải khát
                    </a>
                    <ul class="dropdown-menu" style="width:50px;">
                        @foreach (var item in Model.Where(x => x.maDM == "MDM03"))
                        {
                            var child = Model.Where(x => x.maDM == item.maDM);
                            if (child.Count() > 0)
                            {
                                <li>
                                    <a href="@Url.Action("Index","Home",new { id = item.maDMC})" class="link link--yaku"><span>@Html.DisplayFor(model => item.tenDMC)</span></a>
                                </li>
                            }

                        }
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="btn dropdown-toggle" data-toggle="dropdown" role="button" id="nav-name">
                        Thực phẩm sấy khô
                    </a>
                    <ul class="dropdown-menu" style="width:50px;">
                        @foreach (var item in Model.Where(x => x.maDM == "MDM04"))
                        {
                            var child = Model.Where(x => x.maDM == item.maDM);
                            if (child.Count() > 0)
                            {
                                <li>
                                    <a href="@Url.Action("Index","Home",new { id = item.maDMC})" class="link link--yaku"><span>@Html.DisplayFor(model => item.tenDMC)</span></a>
                                </li>
                            }

                        }
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="btn" role="button" id="nav-name" href="@Url.Action("Blog","Home")">
                        Blog
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>


